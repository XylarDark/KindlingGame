# Skills extras (dormant shelf)

Skills here are **not** loaded by agents. Only `.agents/skills/` is active.

Kindling currently keeps template extras active under `.agents/skills/` instead. To demote one,
move its folder here and correct the always-on count in `AGENTS.md`. See [../README.md](../README.md).
