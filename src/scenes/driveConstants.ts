/** Pin bob cycle on the drive map — shared with HUD screen-space callouts. */
export const PIN_CYCLE_MS = 720;

/** Baked `tex-pin` height — HUD callouts anchor above the chevron tip. */
export const DRIVE_PIN_TEX_H = 80;
/** Live van sprite display height on the drive map. */
export const DRIVE_VAN_TEX_H = 104;
/** Gap between a map anchor and its screen-space sign host. */
export const DRIVE_CHIP_GAP = 14;

/** Design-space chip box limits (passed through {@link scaleMsgBox}). */
export const DRIVE_PIN_MAX_W = 200;
export const DRIVE_PIN_MAX_H = 76;
export const DRIVE_VAN_MAX_W = 300;
export const DRIVE_SHOP_CAP_MAX_W = 240;

/** Tighter plaque margin on projected drive callouts — default sign pads read empty. */
export const DRIVE_CHIP_PAD_X = 8;
export const DRIVE_CHIP_PAD_Y = 6;
